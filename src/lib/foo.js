// modules are not natively supported by any browser or Node, but can transpile to commonjs or amd (for browser).
console.log('=====\n module examples');

var foo = 'boo';
export var foo;
export default foo;